# MySQL values for swiggy_shuttle project
# Based on Bitnami MySQL chart configuration

global:
  storageClass: "niq-sc"
  imageRegistry: "nebulaiqdemo.azurecr.io"

primary:
  enabled: true
  persistence:
    enabled: true
    storageClass: "niq-sc"
    size: 8Gi
    accessModes:
      - ReadWriteOnce
  auth:
    rootPassword: "swiggy_shuttle_root_password_123"
    database: "swiggy_shuttle"
    username: "swiggy_shuttle_user"
    password: "swiggy_shuttle_user_password_123"
  service:
    type: ClusterIP
    port: 3306
  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 500m
      memory: 512Mi 